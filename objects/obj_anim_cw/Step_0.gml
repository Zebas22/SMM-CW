if (tiempo_actual < tiempo_total) {
    // Rotar la imagen
    image_angle += 360 / tiempo_rotacion;
	tiempo_actual++;
    // Disminuir el tamaño usando xscale
}else{
if(x_scale <= 0)
{
	visible = false
	instance_destroy()
	exit
}
	    image_angle += 360 / tiempo_rotacion;
		x_scale -= 0.03

}
